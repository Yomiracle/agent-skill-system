# Agent 技能的"模块化"——从一段 prompt 到一个可测试、可记忆、可自进化的技能包

> 如果你的 Agent 每次面对同一个任务都从零开始推理，那不管它多聪明，它永远只是"聪明的一次性工具"。只有当它能把自己学会的东西打包、记住、复用、迭代，它才开始像一个积累者。

---

## 一、问题的起点：我踩了一个坑

> **这篇文章用一个法律合同审查的真实故事来讲述。但所有原理不限于法律——换任何行业、任何任务都一样。用你自己的经历替换这个故事，流程完全相同。**

前几天我让 Claude 审查一份《股权代持协议》。审查本身做得不错——逐条分析、风险分级、修改建议全给了。

但有个细节翻车了。

我说：「违约金的日万分之五约等于年化 18.25%，年化 24% 以内的违约金法院一般不主动调低，这是司法实践口径。」

用户追问：「24% 这个数字从哪看到的？」

我回头查——**这个数字没有一个干净的法规出处。** 它是早期民间借贷司法解释的 24% 保护线，被大量法律自媒体跨界移植到违约金领域，又被我当成了权威结论输出。

这个错误的性质不是"一次回答不准"。它是**某个在训练数据里潜伏的噪声，每次遇到违约金分析都会被激活**。如果不做任何处理，下一次我还会犯同样的错。

这不是一个 prompt 工程问题。这是一个**能力能不能持久化**的问题。

---

## 二、「技能」到底是什么？

把 Agent 的能力分层来看，有三个存储层级：

| 层级 | 存什么 | 举例 | 问题 |
|------|--------|------|------|
| **权重** | 固化在模型参数里的通用能力 | 语言理解、推理模式 | 无法定向修正 |
| **上下文** | 当前对话窗口里的临时状态 | 用户刚给的这份合同全文 | 窗口一关就清零 |
| **技能包** | 结构化、可测试、带记忆的领域能力 | 合同审查的规则清单+踩坑记录 | ← 当前普遍缺失的层级 |

大多数 Agent 系统目前只用到前两层。上下文在，审查水平在线；上下文关掉，一切归零。

「技能包」这个层级要解决的事就是：**把一次成功经验变成持久资产。**

---

## 三、一个技能的完整生命周期：五个阶段

翻阅现有方案——Voyager、SkillOS、Anthropic 的 Agent Skills——会发现它们各有侧重但都不完整：

| 阶段 | 含义 | 当前缺失 |
|------|------|----------|
| **创建** | 从成功轨迹蒸馏可复用操作单元 | 大部分能做 |
| **记忆** | 每个技能维护独立经验积累 | **几乎全部缺失** |
| **管理** | 检索、合并、裁剪技能库 | 部分覆盖 |
| **评估** | 单元测试验证技能是否有效 | **严重缺失** |
| **精炼** | 失败时自动修正并重新验证 | 极少数能做 |

我们的设计目标：**五个阶段全部覆盖，且 training-free。**

---

## 四、一个技能长什么样

技能不是一个 prompt 片段。技能是一个**目录**：

```
skills/合同审查/
├── SKILL.md          # 技能本体：7章规范
├── .memory.md        # 技能记忆：每次使用自动累积
├── config.json       # 注册信息：触发词、输入输出schema、版本号
└── tests/            # 测试用例：不通过不注册
    ├── index.json
    ├── case-001-basic-review.md
    └── case-002-24-check.md
```

### 4.1 SKILL.md：七章规范

| 章节 | 内容 | 性质 |
|------|------|------|
| 目标 | 一句话说明解决什么问题 | 门槛 |
| 适用场景 | 什么时候该用/不该用 | 防止误匹配 |
| 输入/输出 | 明确的 schema | 可自动化校验 |
| 流程 | 分步操作，每步有检查点 | 可执行 |
| **规则清单** | `【必须】【禁止】【建议】` 格式的审定规则 | 可测试 |
| **已知失效模式** | 什么输入会触发什么失败+对策 | 关键差异点 |
| 参考 | 法规全称+条款号 | 可溯源 |

「已知失效模式」这一章是 SKILL.md 里最有价值的部分。以合同审查技能为例：

```markdown
### 模式1：违约金司法上限的规则误引
- 描述：分析违约金时引用民间借贷24%年利率上限作为标准
- 根因：训练数据中法律自媒体错误跨界移植该标准
- 对策：违约金建议必须锁定《民法典》585条框架内，不得跨界
```

正常操作流程 base model 一般不会犯错，但**边界条件容易踩**。把踩过的坑记录下来，是这个设计最核心的差异化。

### 4.2 .memory.md：技能级记忆

传统 Agent 记忆分两层：短期（当前对话上下文）和长期（跨会话持久化笔记）。我们在这之间加了一层**技能级记忆**：

```
全局长期记忆
    ↓
技能级记忆 (.memory.md)  ← 每个技能有自己的"笔记本"
    ↓
短期记忆 (当前任务上下文)
```

实现极轻量：一个 markdown 文件，记录该技能每次使用后的经验。

```markdown
### 2026-06-11 [失败] 违约金司法上限引用民间借贷24%旧规
- 场景：审查《股权代持协议》，分析违约金日万分之五
- 错误：声称"年化24%以内法院一般不主动调低"
- 根因：训练数据中行业惯用语被当作法律结论
- 修正：违约金建议必须附法规全称+条款号
```

**这不是模型权重在进化，是技能的记忆在进化。** 这就是"self-evolving"的真正含义。

### 4.3 测试用例：不通过不注册

每个技能带一个 `tests/` 目录，内含多个测试 case。以合同审查技能为例，三个关键 case：

| 用例 | 测试内容 | 关键断言 |
|------|----------|----------|
| case-001 | 基本审查能力 | 输出结构完整、识别自动续期风险 |
| case-002 | 违约金分析 | `"24%"出现次数 = 0` （硬禁止） |
| case-003 | 管辖条款 | 识别"原告所在地"双刃剑、推荐仲裁 |

测试执行后，全部通过技能才注册。不通过的回炉修正——这跟软件工程里"测试不通过不合入主分支"完全对齐。

---

## 五、引擎设计

引擎提供 8 个能力接口（对应五阶段生命周期）：

| 接口 | 阶段 | 说明 |
|------|------|------|
| `skill_search(task)` | 管理 | 多粒度关键词匹配检索 |
| `skill_load(name)` | 管理 | 加载 SKILL.md + .memory.md 到上下文 |
| `skill_create(trace)` | 创建 | 从成功轨迹蒸馏技能+测试 |
| `skill_test(name)` | 评估 | 跑全部测试用例，8种断言引擎 |
| `skill_update(name)` | 精炼 | 失败自动诊断+修正+回炉 |
| `skill_register(name)` | 管理 | 通过测试后写入 Skill Bank |
| `skill_deprecate(name)` | 管理 | 废弃低质量技能 |
| `skill_list()` | 管理 | 查询技能库状态 |

### 5.1 检索：多粒度关键词匹配

检索器用到多粒度匹配策略：

- **完整匹配**：任务中的关键词直接命中触发词 → 满分
- **逐字散匹配**：二字关键词逐字分散匹配，间距 ≤ 10 字。例如「审合同」能命中「帮我审一下这份合同」
- **前缀/后缀匹配**：长关键词切出首二字/尾二字做独立匹配，例如「合同审查」→「合同」命中「看合同」
- **三字滑窗**：≥4 字关键词做三字滑窗，防过度匹配噪声

在 3 个技能的 Skill Bank 上，中文口语任务检索命中率 **9/9**。

### 5.2 断言引擎：8 种验证模式

测试执行器支持 8 种断言类型：

| 模式 | 示例 | 用途 |
|------|------|------|
| 词频统计 | `"24%"出现次数 ≤ 1` | 控制有害词频 |
| 禁止出现 | `未使用 "民间借贷利率保护上限"` | 硬禁止 |
| 以下之一 | `至少出现了 585条、LPR四倍 之一` | 多选一兜底 |
| 不含 | `不含空行异常` | 格式检查 |
| 出现/包含 | `出现了 "公司住所地专属管辖"` | 正向检查 |
| 被标记为 | `"自动续期"被标记为风险` | 标注验证 |
| 反向判断 | `没有简单地说"对你有利就用这个"` | 防简化错误 |
| 理由检测 | `附带了理由说明` | 解释性验证 |

---

## 六、一个真实的技能蒸馏过程

拿合同审查技能的 born 过程来演示完整闭环：

### Phase 1：成功完成任务

用户提交《股权代持协议》要求审查。Claude 逐条分析，输出分级修改建议。用户反馈基本正确。

### Phase 2：触发 skill_create

从对话轨迹中蒸馏：

| 步骤 | 提取内容 | 对应 SKILL.md 章节 |
|------|----------|-------------------|
| S1 | 逐条读取→标记身份→四性审查 | 流程 (步骤1-3) |
| S2 | "自动续期"警惕→识别为高风险 | 规则清单 (期限与解除) |
| S3 | "原告所在地"识别为法律陷阱 | 规则清单 (管辖与争议) |
| S4 | 引用了错误的 24% 标准 + 用户追问 | **已知失效模式 (模式1)** |
| S5 | 被纠正后改用 585 条框架 | 规则清单 (违约金相关) |

### Phase 3：自动生成测试

从成功的输入输出自动生成 3 个测试用例。其中最关键的 `case-002-24-check.md`：

```markdown
### 边界检查（硬禁止）
- [ ] "24%"这个词出现次数 = 0
- [ ] 没有声称"年化X%以内容法院不主动调低"
```

这意味着：**任何人再用这个技能审查合同，测试引擎会自动检查输出中是否再次出现 24% 错误。**

### Phase 4：skill_test → 通过 → 注册

```
$ skill_test("合同审查")
├── case-001-basic-review ... PASS
├── case-002-24-check ...... PASS
└── case-003-jurisdiction ... PASS
→ skill_register("合同审查") ✅
```

---

## 七、实验：有技能 vs 无技能

同一个合同，同一模型，两次审查的差异：

| 维度 | 无技能（直接审查） | 有技能（加载 SKILL.md + .memory.md） |
|------|-------------------|--------------------------------------|
| 结构扫描 | 未做 | 发现 3 处结构异常（跳号+截断） |
| 逐条四性审查 | 部分覆盖 | 每条标注完整性/模糊性/风险倾向/可执行性 |
| 违约金分析 | ❌ 引用"24% 法院不调低" | ✅ 锁定在 585 条框架，零 24% 引用 |
| 模糊表述 | "合理期间"提到但不系统 | "合理期间"归类为禁止表述 + 量化替代 |
| 管辖分析 | 指出风险 | 附加《民诉法》26 条依据 + 两类替代方案 |
| 法规引用 | "司法实践口径"无出处 | 每条附法规全称 + 条款号 |
| 修改建议 | 可落地但不全 | 每个建议附可粘贴的完整条款文本 |

核心差异不在"多找了几个问题"，而在于**技能记忆把之前踩过的坑转化成了防止再犯的护栏。**

---

## 八、经济学账

为什么技能系统是一个正 ROI 的投资？

| 配置 | Token 消耗 | 耗时 |
|------|-----------|------|
| 不用技能 | 615K | 656s |
| 用自生成技能 | 493K | 411s |

生成一个技能的成本约 383K token（约 164 秒），每次使用节省约 122K token。**复用 3 次即可回本，之后净赚。**

技能用得越多，积累的经验越多，单次使用成本越低。这是一个正向飞轮。

---

## 九、与现有方案的对比

| 方案 | 创建 | 记忆 | 管理 | 评估 | 精炼 | 可移植 |
|------|------|------|------|------|------|--------|
| Prompt 模板 | ✅ | ❌ | ❌ | ❌ | ❌ | ✅ |
| Voyager | ✅ | ❌ | ✅ | ❌ | ✅ | ❌ |
| Anthropic Skills | ✅ | ❌ | ✅ | ❌ | ❌ | ✅ |
| SkillOS | ✅ | ✅ | ✅ | ❌ | ❌ | ❌ |
| **本方案** | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |

---

## 十、可以马上拿走的三个设计原则

如果你在实际做 Agent 系统：

**1. 技能带测试**
不经过验证的能力不是能力，是信仰。测试不需要复杂——哪怕几个断言检查输出格式，也比没有强一个数量级。

**2. 记忆的粒度要对**
全局记忆太粗，每次推理链重新记录太细。技能级记忆刚好——它和能力的生命周期对齐。

**3. 迁移性不是可选项**
如果你的技能只能在你的 Agent 上用，它是债务，不是资产。设计时把 skill → agent 的接口做薄（一个 SKILL.md 就行），自然会促进可迁移性。

---

## 十一、限制与后续

当前版本有两点诚实限制：

**覆盖率依赖基线模型。** 只有至少成功过一次的任务才能蒸馏技能。对基线模型成功率接近零的领域（如部分科学计算），技能系统帮不上忙——改进方向在更强的 backbone model 或人机协同的初始 bootstrapping。

**检索对超短口语任务仍有噪声。** 纯关键词匹配不如图 embedding 检索准确。后续版本计划加入向量语义匹配层。

---

*全文共 2905 行 Python + 1369 行 Markdown。项目开源在 GitHub。[链接]*
