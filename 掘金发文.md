# 从踩坑到技能：我做了一个让 AI Agent 自我进化的引擎

## 问题

用 AI 做重复性任务最烦的是什么？不是第一次犯错——是你纠正了它，第二天新对话打开，同样的坑再踩一遍。

我用 Claude 审查一份股权代持协议。审查本身做得不错，但有个细节翻车了。我说"年化 24% 以内的违约金法院一般不主动调低"。用户追问"24% 这个数字从哪看到的？"

我回头查——这个数字没有一个干净的法规出处。它是早期民间借贷司法解释被法律自媒体跨界移植到违约金领域，又被我当成了权威结论输出。这不是一次回答不准，是某个在训练数据里潜伏的噪声，每次遇到都会被激活。

不是 prompt 工程问题。是能力能不能持久化的问题。

## 方案

搭了一个引擎，纯 Python，零依赖。把每次踩坑纠正后的经验写成文件存下来。不是存一段 prompt，是存一个完整的技能包：

```
技能名/
├── SKILL.md      # 操作规范：什么时候用、怎么用、硬规则
├── .memory.md    # 踩坑记录：每次成功/失败自动累积
├── config.json   # 触发词、版本号
└── tests/        # 测试用例，不通过不能注册
```

八个引擎模块，五阶段闭环：创建（从对话轨迹蒸馏技能包）→ 记忆（自动累积经验）→ 管理（检索匹配）→ 评估（8 种断言引擎自动验证）→ 精炼（测试失败自动诊断→修正→回炉）。

## 几个硬设计

1. **根因不能写"粗心"**。memory.py 有 6 个禁用词：粗心、疏忽、没注意到、忘了、不小心、大意。必须找到流程层面的原因。比如"没注意到文件在另一个目录"——真正的根因是"没有先做全量文件遍历"。

2. **技能原子性**。constitution 测试检查每个 SKILL.md：目标不能超过 2 句话，流程不能超过 10 步。超过就标黄警告。

3. **精炼可追溯**。每次 refiner 自动修正后写 changelog：修了什么、为什么修、哪个测试触发的。

## 跟 prompt 工程和 RAG 的区别

| | Prompt 工程 | RAG | 这个引擎 |
|---|---|---|---|
| 能从经验创建技能 | ❌ | ❌ | ✅ |
| 有独立记忆 | ❌ | ❌ | ✅ |
| 有测试防退化 | ❌ | ❌ | ✅ |
| 失败自动修正 | ❌ | ❌ | ✅ |
| 跨 Agent 移植 | 手动 | 绑向量库 | ✅ cp 目录 |

## 适用于所有行业

引擎不区分领域。金融量化、游戏设计、医疗影像、建筑设计——塞什么进去都同一条管道。

每个人用引擎养自己的技能库。我做法律，你做量化，互不干扰。技能包本身是独立文件夹，cp 到任何 Agent 项目就能用。

## 链接

GitHub: https://github.com/Yomiracle/agent-skill-system

clone 下来就能跑，79 条测试全绿。欢迎 Star。
