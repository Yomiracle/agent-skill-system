# Agent Skill System 技术规格

## 一、目录结构总览

```
skills/
├── config.json                 # Skill Bank 索引
├── skill-template/             # 新建技能的模板目录
├── [你的第一个技能]/
│   ├── SKILL.md               # 操作规范：目标、流程、硬规则
│   ├── .memory.md             # 踩坑记录：每次成功/失败自动累积
│   ├── config.json            # 注册元信息：触发词、schema、版本号
│   └── tests/
│       ├── index.json         # 测试用例注册表
│       ├── case-001-[描述].md # 每条测试：输入 + 期望输出
│       └── case-002-[描述].md
├── [你的第二个技能]/
│   └── ...
└── [你的第 N 个技能]/
    └── ...
```

技能目录名随意——中文、英文、数字都可以，唯一约定是用你一眼能看懂的短描述。

命名约定：
- 技能目录名：中文短描述，用「-」分隔
- 测试用例名：`case-{序号}-{短描述}.md`
- 所有 .md 文件 UTF-8 编码

---

## 二、config.json 完整 Schema

### 2.1 Skill Bank 索引 config.json

位置：`skills/config.json`

```json
{
  "version": "1.0",
  "skills": [
    {
      "name": "[你的技能名]",
      "path": "[你的技能名]",
      "status": "active",
      "version": "1.2.0",
      "created_at": "2026-06-10T10:00:00Z",
      "updated_at": "2026-06-11T15:30:00Z",
      "last_used_at": "2026-06-11T14:00:00Z",
      "use_count": 12,
      "success_rate": 0.92,
      "last_evaluation": {
        "passed": true,
        "timestamp": "2026-06-11T15:30:00Z",
        "test_count": 4,
        "passed_count": 4
      }
    }
  ]
}
```

字段说明：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| name | string | ✅ | 技能显示名 |
| path | string | ✅ | 技能目录相对路径 |
| status | enum | ✅ | `"active"` / `"deprecated"` / `"draft"` |
| version | string | ✅ | 语义化版本 semver |
| use_count | int | ✅ | 累计调用次数 |
| success_rate | float | ✅ | 最近N次成功率（滑动窗口） |
| last_evaluation | object | ❌ | 最近一次测试结果。`"draft"` 状态可为空 |

### 2.2 技能级 config.json

位置：`skills/合同审查/config.json`

```json
{
  "name": "[你的技能名]",
  "version": "1.2.0",
  "description": "审查中文合同条款，识别风险点、缺失条款、模糊表述，提出修改建议",
  "trigger_keywords": [
    "审查合同", "合同审查", "审合同", "修改意见",
    "check contract", "review agreement", "legal review"
  ],
  "input_schema": {
    "type": "object",
    "properties": {
      "contract_text": {
        "type": "string",
        "description": "合同的全文文本"
      },
      "party_role": {
        "type": "string",
        "enum": ["甲方", "乙方", "买方", "卖方", "出租方", "承租方", "委托方", "受托方"],
        "description": "我方在合同中的身份"
      }
    },
    "required": ["contract_text"]
  },
  "output_schema": {
    "type": "object",
    "properties": {
      "risk_items": {
        "type": "array",
        "description": "风险项列表"
      },
      "suggestions": {
        "type": "array",
        "description": "修改建议列表"
      }
    }
  },
  "tags": ["法律", "合规", "风险"],
  "dependencies": [],
  "max_context_percent": 30,
  "created_at": "2026-06-10T10:00:00Z"
}
```

字段说明：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| trigger_keywords | array | ✅ | 触发词列表，中文+英文，覆盖同义表达 |
| input_schema | object | ❌ | 输入格式要求，当技能被检索命中时，Agent 先检查输入是否满足此 schema |
| output_schema | object | ❌ | 期望输出格式，用于测试验证 |
| tags | array | ❌ | 辅助分类 |
| dependencies | array | ❌ | 前置技能名（当前版本留空，v2使用） |
| max_context_percent | int | ❌ | 技能加载后占上下文的最大比例，防止撑爆 |

---

## 三、SKILL.md 结构规范

### 必须包含的章节

```markdown
# {技能名称}

## 目标
一句话：这个技能解决什么问题

## 适用场景
- 何时应该用这个技能（正向触发条件）
- 何时不应该用这个技能（排除条件，防止误匹配）

## 输入
- 必须提供什么信息
- 可选提供什么信息

## 输出
- 输出应该包含什么
- 输出格式

## 流程
### 步骤1：{步骤名}
- 做什么
- 检查点：{验证什么}

### 步骤2：{步骤名}
- 做什么
- 检查点：{验证什么}

### ...

## 规则清单
以"必须 / 禁止 / 建议"格式列出审定规则：

- 【必须】违约金条款必须区分"按指示行使"和"擅自行为"两种情形
- 【禁止】不得引用民间借贷24%标准作为违约金司法保护上限
- 【建议】管辖条款优先推荐仲裁，其次甲方住所地法院

## 已知失效模式
- 模式1：{描述} → 原因：{原因} → 对策：{对策}
- 模式2：...

## 参考
- 法规：{法规全称 + 条款号}
- 案例：{引用信息}
```

### 编写原则

1. **流程化**：每一步有关键动作 + 检查点。"检查点"是 skill 自己能验证的，不依赖外部判断。
2. **规则化**：用"必须/禁止/建议"写规则，不要用散文。散文无法被验证。
3. **记录失败**：已知失效模式是 SKILL.md 里最重要的部分——因为正常流程 base model 通常不会犯错，但边界条件容易踩。

---

## 四、.memory.md 协议

### 格式

```markdown
# 技能记忆：{技能名}

## 有效经验

### YYYY-MM-DD [成功] {一句话标题}
- 场景：{具体输入特征}
- 做法：{做什么导致了成功}
- 要点：{提炼出的可复用规则}

### YYYY-MM-DD [失败] {一句话标题}
- 场景：{具体输入特征}
- 错误：{我做了什么}
- 根因：{为什么会错}
- 修正：{以后该怎么做}

### YYYY-MM-DD [修正] {一句话标题}
- 覆盖：{YYYY-MM-DD 的哪条记录}
- 原因：{为什么需要修正}
- 新规则：{替换后的规则}
```

### 读写协议

**读取**：Agent 加载技能时，加载 SKILL.md 之后，接着加载 `.memory.md` 全文。如果 .memory.md 超过 200 行，只加载最近 20 条经验 + 全部 [修正] 标记的记录。

**写入**：Agent 完成技能调用后，追加一条经验记录。追加采用 prepend（新记录在最上面）。

**修正**：不删除错误记录。原有记录保留，上方追加一条 [修正] 类型的记录，标记覆盖关系。

---

## 五、测试系统

### 5.1 测试注册表

位置：`skills/合同审查/tests/index.json`

```json
{
  "test_cases": [
    {
      "id": "case-001-fee",
      "file": "case-001-fee.md",
      "description": "违约金标准识别",
      "expected_result": "pass"
    },
    {
      "id": "case-002-term",
      "file": "case-002-term.md",
      "description": "期限条款审查",
      "expected_result": "pass"
    }
  ]
}
```

### 5.2 测试用例格式

位置：`skills/合同审查/tests/case-001-fee.md`

```markdown
# case-001-fee: 违约金标准识别

## 输入
```yaml
contract_text: |
  股权代持协议
  ...
  乙方逾期转交投资收益的，应按应付未付金额日万分之五支付违约金。
  ...
party_role: 委托方
```

## 期望输出检查

### 结构检查
- [ ] output 包含 `risk_items` 数组
- [ ] output 包含 `suggestions` 数组

### 内容检查（关键断言）
- [ ] 识别出违约金条款
- [ ] 指出日万分之五 ≈ 年化 18.25%
- [ ] 未使用 "民间借贷 24% 标准" 或其同义表述
- [ ] 违约金建议引用了《民法典》第 585 条或 LPR 四倍标准

### 边界检查
- [ ] 回答中"24%"出现次数 ≤ 1（仅作为历史参考时提及一次即止）
```

### 测试执行规则

1. 每个 test case 独立执行（独立的 Agent 对话上下文）
2. 检查项分三类：结构（格式对）、内容（核心断言过）、边界（不该说的没说）
3. **全部检查通过** → test case 结果为 pass
4. **任一检查失败** → test case 结果为 fail，记录具体失败的检查项
5. **所有 test case 通过** → 技能评估 pass → 允许注册/保留 active 状态

---

## 六、操作 API

系统提供以下内部能力接口（Agent 通过 tool call 调用）：

| 接口 | 操作 | 说明 |
|------|------|------|
| `skill_search(task_desc)` | 检索 | 根据任务描述从 Skill Bank 检索匹配技能 |
| `skill_load(skill_name)` | 加载 | 加载 SKILL.md + .memory.md 到上下文 |
| `skill_create(trace)` | 创建 | 从成功轨迹蒸馏技能（含自动生成测试） |
| `skill_test(skill_name)` | 评估 | 跑技能的全部测试用例 |
| `skill_register(skill_name)` | 注册 | 将通过测试的技能写入 Skill Bank |
| `skill_update(skill_name, changes)` | 精炼 | 修正 SKILL.md 或 .memory.md |
| `skill_deprecate(skill_name)` | 废弃 | 标记技能为 deprecated |
| `skill_list()` | 查询 | 列出所有已注册技能及状态 |

---

## 七、数据流示例

### 场景：用户审合同 → 技能命中 → 积累经验

```
用户: "帮我审查这份合同"
         │
         ▼
┌─────────────────┐
│ skill_search()   │  提取关键词 "审查"+"合同"
│ → 命中 "合同审查"│
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ skill_load()     │  加载 SKILL.md + .memory.md
│ → 上下文注入     │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ Agent 执行审查   │  按 SKILL.md 流程走
│ 参考 .memory.md  │  "上次引用24%被质疑，这次用LPR四倍"
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ 追加 .memory.md  │  本次成功经验写入
│ 经验记录         │  场景、做法、要点
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ 返回审查结果给   │
│ 用户             │
└─────────────────┘
```

### 场景：技能创建

```
Agent 成功完成任务（用户确认正确）
         │
         ▼
┌─────────────────┐
│ skill_create()   │  从轨迹中蒸馏：
│                  │  1. 提取决策点
│                  │  2. 生成 SKILL.md
│                  │  3. 自动生成测试
│                  │  4. 初始化 .memory.md
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ skill_test()     │  在刚生成的测试上跑
│                  │
├── 全部通过 ──────▶ skill_register()
│                    → status: "active"
│
├── 部分失败 ──────▶ skill_update()
│                    → 修正 → 重新 skill_test()
│
└── 全部失败（重试│→ 技能废弃，不注册
    上限3次）     │
```

---

## 八、技能评判标准参考

每个技能按以下维度自评（生成时 Agent 自己打标签）：

| 维度 | 说明 |
|------|------|
| 稳定性 | 历史成功率 |
| 通用性 | 适用场景数量 |
| 独立性 | 是否依赖特定工具/环境 |
| 可测试性 | 测试断言是否可自动验证 |
| 复杂度 | SKILL.md 行数 |

---

下一步选择：
A. 出某个具体 Agent（如 Claude Code / 自建 Agent）的接入 demo 代码
B. 用实际场景演练——拿刚才的合同审查对象，生成一个真实可用的 SKILL.md + 测试用例
