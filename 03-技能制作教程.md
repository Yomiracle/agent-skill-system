# 技能制作教程 — 从一次对话到一个可复用的技能

本教程演示了创建第一个技能的全过程。你不需要有任何编程基础——整个过程就是 Agent 按你的指示写几个 markdown 文件，引擎自动验证。

下面用三个不同领域的例子来展示通用流程。**你只需要替换例子里的行业术语为你自己的场景。**

---

## 教程概览

| 步骤 | 做什么 | 谁来做 |
|------|--------|--------|
| 1 | 正常做一次任务，踩坑、纠正、直到正确 | 你和 Agent |
| 2 | 说"把这个做成技能" | 你 |
| 3 | Agent 从对话轨迹蒸馏出 SKILL.md + 测试 | Agent（creator.py） |
| 4 | 引擎自动跑测试验证 | 引擎 |
| 5 | 测试通过 → 注册到技能库 | 引擎 |

---

## 步骤 1：正常做一次任务

### 例子 A：法律 — 审查合同

你让 Agent 审查一份合同。Agent 给出的违约金分析引用了"年化 24% 以内法院不调低"。你纠正它："这个 24% 没有干净的法条出处，应该引《民法典》第 585 条，用 LPR 四倍标准。"

Agent 改对了。这次对话里有一个**可提取的规则**：违约金分析 → 禁止引用 24% 旧标准 → 应引用民法典 585 条。

### 例子 B：软件开发 — 代码审查

你让 Agent 审查一段 Python 代码。Agent 说"这个函数太长，建议拆分"。你纠正它："不要只说'太长'，给一个硬标准——超过 50 行就标记，同时指出具体哪个逻辑块可以独立成函数。"

Agent 按你的标准重新审查了。同样，这次对话里有一条可提取的规则。

### 例子 C：数据分析 — 数据清洗

你让 Agent 做数据清洗。Agent 看到缺失值就直接删除了所有行。你纠正它："缺失值不要上来就删。先统计缺失比例，<5% 删行，5-30% 用中位数填充，>30% 单独标记为'高缺失'列为新特征。"

Agent 照做了。同样，一条规则诞生了。

---

## 步骤 2：说"把这个做成技能"

不管哪个场景，指令都一样：

> "把刚才的对话经验做成技能，叫 [技能名]"

你不需要写任何代码——Agent 会从对话轨迹中识别决策点，填空生成 SKILL.md。

---

## 步骤 3：Agent 蒸馏出技能

蒸馏的本质就是从对话中提取四样东西：

| 提取什么 | 例子 A（法律） | 例子 B（开发） | 例子 C（数据） |
|---------|-------------|-------------|-------------|
| **触发条件** | 用户要求审查中文合同 | 用户要求审查 Python 代码 | 用户要求清洗数据 |
| **流程步骤** | 确认身份 → 扫描结构 → 逐条四性审查 → 分级输出 | 扫描文件 → 逐函数检查 → 按标准标记 → 给出重构建议 | 统计缺失 → 按规则填充/标记 → 输出清洗报告 |
| **硬规则** | ❌ 不得引用 24% 旧标准 | ❌ 不得仅说"太长"而不给硬标准 | ❌ 不得上来就删缺失行 |
| **已知失效模式** | 违约金分析容易引用 24% | 函数长度评价过于主观 | 缺失值处理过于粗暴 |

Agent 用这些信息填空生成 SKILL.md，生成对应的测试用例，然后调用 `creator.py` 验证。

---

## 步骤 4-5：引擎验证 + 注册

```bash
# Agent 自动执行：
python3 engine/cli.py health "[你的技能名]"
# 输出: Pass rate: 100% (N/N) → 注册成功
```

如果某条测试不通过，`refiner.py` 会自动进入精炼循环：诊断失败断言 → 修正 SKILL.md → 重新跑测试 → 最多 3 轮。

---

## 完整的技能目录（以例子 A 为例）

```
skills/合同审查/
├── SKILL.md
│   ├── ## 目标：审查中文合同，按指定方视角识别风险
│   ├── ## 触发条件：用户说"审合同""审查""看有没有坑"
│   ├── ## 流程：确认身份 → 扫描 → 逐条审查 → 分级输出
│   ├── ## 硬规则：❌ 不得引用 24% 旧标准
│   └── ## 已知失效模式：违约金分析 → 易触发训练数据中的 24%
├── .memory.md
│   └── ### 2026-06-11 [失败] 违约金引用了民间借贷 24% 旧规 → 已修正
├── config.json
│   └── trigger_keywords: ["审合同", "审查", "风险", "review contract"]
└── tests/
    ├── index.json
    ├── case-001-违约金引用585条.md
    └── case-002-管辖条款识别.md
```

---

## 技能每次使用后都会变聪明

```
第一次：5 条硬规则，2 条已知失效模式
  ↓ 使用了 20 次，踩了 3 个新坑
  ↓ .memory.md 累积了 3 条失败记录 + 15 条成功经验
  ↓ SKILL.md 硬规则从 5 条增长到 8 条
  ↓ 测试用例从 2 个增长到 6 个
  ↓ 版本从 1.0.0 升级到 1.2.0

第二十次：8 条硬规则，5 条已知失效模式，6 条测试
```

**技能在自我进化——不是模型权重在变，是规则和记忆在积累。**

---

## 你要做的事

1. 正常用 Agent 做一次任务
2. 你说"把这个做成技能，叫 X"
3. Agent 写文件，引擎验证，注册

所有的 json、yml、测试生成都是自动的。你只需要知道自己的行业规则是什么。
