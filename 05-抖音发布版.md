# 怎么让 AI 记住经验，下次不犯同样的错

我搭了一套方案，跑通了。说怎么做。

---

## 问题是啥

用 AI 审合同，它把违约金标准说错了。纠正之后，下次打开新对话，它还会犯。

因为它的经验存在对话窗口里，窗口关了，经验清零。

---

## 我的方案

把成功的操作经验，写成文件存起来。下次 AI 干活前先读这些文件。

一个技能就是一个文件夹，里面四个文件：

```
合同审查/
├── SKILL.md      — 怎么审的，步骤和规则
├── .memory.md    — 踩过什么坑，怎么修的
├── config.json   — 什么时候该用这个技能
└── tests/        — 测试用例，验证它没退化
```

---

## SKILL.md 怎么写

分七部分。举个真实例子：

**目标**：审查中文合同，按指定方视角识别风险

**流程**：
1. 确认我方身份
2. 扫描合同结构（有没有编号跳跃、条款断尾）
3. 逐条四性审查（完整性、模糊性、风险倾向、可执行性）

**规则清单**：
- 【必须】违约金分析引用《民法典》585 条
- 【禁止】不得引用"年化 24% 以内法院不调低"
- 【建议】管辖条款优先选仲裁

**已知失效模式**：
- 审查违约金时容易引用 24% 旧标准 → 补一条【禁止】规则

---

## .memory.md 怎么写

每次用完后追加一条。只记实战中有用的东西：

```
### 6月11日 [失败] 违约金引用了24%旧规
- 场景：股权代持协议审查
- 错误：说"24%以内法院不调低"
- 根因：训练数据噪声被当成法律结论
- 修正：违约金建议必须锁定585条
```

下次 AI 加载技能时，会先读这个文件，不会再犯。

---

## 测试用例怎么弄

就是验证 AI 的输出对不对。举个例子：

输入：合同里有"日万分之五"违约金条款  
期望输出检查：  
- "24%"这个词出现了吗？→ 应该是 0 次  
- 提到"民法典 585 条"了吗？→ 应该有  
- 提到"LPR 四倍"了吗？→ 应该有  

跑不过的，回炉改 SKILL.md，直到通过。

---

## 怎么用

**创建技能**  
先把 SKILL.md 写了 → 放 tests 用例 → 注册到技能库

**使用技能**  
用户说"审合同" → 自动匹配 合同审查 技能 → 加载 SKILL.md + .memory.md → AI 按规范执行 → 自动追加经验到 .memory.md

**技能坏了**  
测试挂了 → 自动诊断是 SKILL.md 有问题还是 .memory.md 有误导 → 修正 → 重跑测试

---

## 代码

引擎写了 8 个模块：检索 / 加载 / 记忆管理 / 测试执行 / 精炼 / 自动创建 / Bank 管理 / CLI

命令行跑：

```bash
# 看有哪些技能
python3 engine/cli.py list

# 检索匹配
python3 engine/cli.py search "审合同"

# 跑测试
python3 engine/cli.py test -m "合同审查"

# 健康诊断
python3 engine/cli.py health "合同审查"
```

完整代码在 `agent-skill-system/`，Python 纯库不依赖特定框架。

---

## 和现有方案的对比

Anthropic Agent Skills：有创建和管理，没有记忆和测试  
Voyager：有创建和精炼，没有记忆和评估  
SkillOS：有创建和记忆，没有测试和精炼  
**这个方案：五个阶段全有**

---

## 一件事

目前技能库里有 3 个技能：合同审查 / 论文审查 / Bug诊断。都是从真实任务蒸馏出来的，测试全部通过。

```
合同审查      2/3 tests  7条记忆
论文审查      2/2 tests  1条记忆
Bug诊断与修复  3/3 tests  4条记忆
```

---
*代码在 GitHub，评论区见*
