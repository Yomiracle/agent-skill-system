# Agent Skill System 设计方案

## 一、一句话定位

把 Agent 在任务中积累的成功经验，转化为**可复用、可测试、可记忆、可自进化的技能资产**。

---

## 二、核心概念

### 技能 (Skill) 究竟是什么？

不是一段 prompt。不是一段代码。不是一个工具。

技能是一个**目录**，包含以下文件：

```
skills/
└── 合同审查/
    ├── SKILL.md          # 技能本体：目的、流程、规则
    ├── .memory.md        # 技能记忆：踩坑记录、边界条件
    ├── config.json       # 元信息：版本、依赖、触发条件
    └── tests/
        ├── case-01.md    # 测试用例
        └── case-02.md
```

| 文件 | 性质 | 问题 |
|------|------|------|
| SKILL.md | 静态知识 | 「这个任务该怎么做」 |
| .memory.md | 累积经验 | 「上次哪里出过问题」 |
| config.json | 注册表 | 「什么时候该用我」 |
| tests/*.md | 质量锚 | 「我是不是还是对」 |

---

## 三、五阶段生命周期

```
创建 ──→ 评估 ──→ 注册 ──→ 使用 ──→ 精炼
  ↑                                       │
  └───────────────────────────────────────┘
```

### 阶段一：创建 (Create)

**触发条件**：Agent 完成了一个任务，且用户确认结果是正确的。

**过程**：
1. Agent 从成功轨迹中提取关键决策点
2. 将决策点结构化为 SKILL.md：
   - 目标：这个技能解决什么问题
   - 输入/输出 schema
   - 分步流程（每步有检查点）
   - 已知陷阱和失效模式
3. 自动生成测试用例（基于刚跑通的输入）

**关键原则**：不把一次偶然成功当成规律。创建阶段只记录「验证过的操作序列」，不猜测泛化边界。

---

### 阶段二：评估 (Evaluate)

**触发条件**：技能创建完成后，必须立即跑测试。

**过程**：
1. 在每个 test case 上独立执行技能
2. 检查：输出格式对不对？关键断言是否通过？边界条件有没有命中？
3. 全部通过 → 进入注册阶段
4. 任何失败 → 触发精炼（回到创建阶段修正）

**关键原则**：测试不通过不注册。这跟代码不进 main 分支一个道理。

---

### 阶段三：注册 (Register)

**触发条件**：全部测试通过。

**过程**：
1. 技能写入 Skill Bank（持久化目录）
2. 生成 config.json：技能名、适用场景描述、触发关键词、输入格式要求
3. 初始 .memory.md 为空

**关键原则**：注册只是一种「该技能被加载到搜索范围」的状态，不代表永久有效——后续使用失败可以卸载。

---

### 阶段四：使用 (Use)

**触发条件**：Agent 收到新任务，在 Skill Bank 中检索到匹配的技能。

**过程**：
1. 通过 config.json 的关键词/语义匹配检索候选技能
2. 加载 SKILL.md + .memory.md 到 Agent 上下文
3. Agent 按 SKILL.md 流程执行，同时参考 .memory.md 里的经验
4. 执行完成后，将此次执行的经验（成功/失败/边界发现）追加到 .memory.md

**关键原则**：使用即学习。每次调用都是一次记忆更新机会。

---

### 阶段五：精炼 (Refine)

**触发条件**：
- 测试失败（评估阶段触发）
- 使用失败（用户反馈 / Agent 自检）

**过程**：
1. 分析失败原因：是 SKILL.md 逻辑有误，还是 .memory.md 里积累了坏经验？
2. 修正对应部分
3. 重新跑全部测试
4. 直到通过或达重试上限

**关键原则**：精炼不改 skill 方向。如果基础假设错了，应该注销技能重新创建。

---

## 四、三个关键设计决策

### 4.1 技能带测试——不通过不注册

测试不需要多复杂。最低限度：

```markdown
# 测试用例：合同审查-违约金条款识别

## 输入
```
乙方逾期付款，应按日万分之五支付违约金。
```

## 期望输出
- 识别到违约金条款：是
- 违约金标准：日万分之五
- 年化换算：约18.25%
- 风险提示：是否触发
```

测试追求的不是「覆盖所有边界」，而是「验证技能核心逻辑不会退化」。

---

### 4.2 技能级记忆——每个技能有自己的笔记本

`.memory.md` 的写入格式：

```markdown
## 经验记录

### 2026-06-11 [成功]
- 场景：违约金日万分之五的条款
- 结论：引用民间借贷24%旧规无效，应改用LPR四倍或民法典585条
- 触发条件：当条款中出现带百分比的违约金数字时

### 2026-06-12 [失败]
- 场景：用户追问24%来源
- 原因：引用了行业惯用语而非法规原文
- 修正：以后回答必须附法规全称+条款号
```

**记忆的生命周期**：追加式增长。如果发现某条记忆有害，不是删除（可能丢失信息），而是追加一条修正记录。

---

### 4.3 技能间独立——不组链、不嵌套

每个技能解决一个独立的任务类型，技能和技能之间不构成调用链（skill A 调 skill B）。理由：

- 调用链会引入级联故障
- 拆得过细的技能在实际使用中难以被正确检索
- Agent 自己会在执行时判断是否需要加载多个技能

---

## 五、Skill Bank 的检索机制

### 检索流程

```
用户任务 → 提取关键词 → 匹配 config.json → 语义相似度排序 → 返回 Top-N
```

### 匹配维度

| 维度 | 权重 | 说明 |
|------|------|------|
| 关键词命中 | 高 | config.json 中的 trigger_keywords |
| 语义相似度 | 中 | 任务描述和 skill 描述之间的 embedding 距离 |
| 使用频率 | 低 | 用过的技能优先（历史有效性信号） |
| 最近失败 | 负向 | 最近一次使用失败的技能降权 |

---

## 六、技能的可移植性

技能是一个**独立目录**，不绑任何 Agent 框架。迁移时：

```
cp -r skills/合同审查/ target-agent/skills/
```

唯一要求：目标 Agent 能读取 markdown 并按 SKILL.md 的流程执行。这是你文章里说的「把 skill → agent 的接口做薄」。

---

## 七、与现有方案的对比

| 方案 | 创建 | 记忆 | 评估 | 精炼 | 可移植 |
|------|------|------|------|------|--------|
| 普通 prompt 模板 | ✅ | ❌ | ❌ | ❌ | ✅ |
| Voyager | ✅ | ❌ | ❌ | ✅ | ❌ |
| Anthropic Skills | ✅ | ❌ | ❌ | ❌ | ✅ |
| SkillOS | ✅ | ✅ | ❌ | ❌ | ❌ |
| **本方案** | ✅ | ✅ | ✅ | ✅ | ✅ |

---

## 八、不做的事情

以下有意不纳入当前方案：

1. **不做多轨迹融合创建**：当前版本单轨迹创建，多轨迹融合是 v2 的优化方向
2. **不做技能间自动组合**：不建技能链，避免引入不必要的复杂度
3. **不做实时测试**：测试只在注册和精炼时跑，不在每次调用时跑（性能考虑）
4. **不做 memory 可信度评分**：当前 .memory.md 的修正靠追加，不靠打分。可信度系统是 v2 方向

---

## 九、实现路线图

| 阶段 | 内容 | 里程碑 |
|------|------|--------|
| M0 基础 | Skill 目录结构定义、SKILL.md 模板、config.json schema | 可手动创建技能 |
| M1 使用 | Skill Bank 检索、技能加载到上下文、基础使用流程 | Agent 能用已注册的技能 |
| M2 记忆 | .memory.md 自动读写、追加式经验记录 | 技能会"记住"踩过的坑 |
| M3 创建 | 从成功轨迹蒸馏技能、自动生成测试用例 | Agent 能自己生技能 |
| M4 评估 | 测试执行引擎、通过/失败判断、阻断注册 | 不过测试的技能不能上 |
| M5 精炼 | 失败自动分析、skill 修正循环 | 坏掉的技能能自己修 |

---

## 十、一个最简原型的样子

如果今天就要跑起来，最小可行系统只需要：

```
skills/
  config.json            ← Skill Bank 索引
  合同审查/
    SKILL.md             ← 技能流程
    .memory.md           ← 空文件
```

Agent 启动时加载 config.json 作为 skill 目录。收到任务 → 关键词匹配 → 加载对应 SKILL.md 到上下文。

这就跑通了「使用」阶段。其他阶段可以一个一个往上加。

---

下一步：出详细的技术规格（config.json 的完整 schema、SKILL.md 的章节规范、测试用例格式、.memory.md 的协议），还是先出某个具体 Agent 的接入 demo？
